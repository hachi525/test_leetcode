//
// Created by admin on 2022/7/30.
//

#include "L952_largestComponentSize.h"

/* 给定一个由不同正整数的组成的非空数组nums ，考虑下面的图：
 * 有nums.length个节点，按从nums[0]到nums[nums.length - 1]标记；
 * 只有当nums[i]和nums[j]共用一个大于 1 的公因数时，nums[i]和nums[j]之间才有一条边。
 * 返回 图中最大连通组件的大小
 * */

/* 方法一：暴力建图遍历，超时
 * 方法二：并查集+欧拉筛/埃及筛找素数
 * */
int L952_largestComponentSize::largestComponentSize(vector<int> &nums) {

}

int main() {

    return 0;
}
